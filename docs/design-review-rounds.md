# יומן סבבי ביקורת עיצוב — הלולאה עם "קלוד דיזיין"

מסמך הרציפות של הלולאה (ראו `agent-model-policy.md`). כל סבב: ממצאים ←
מה יושם ← מה נדחה ולמה. מבקר חדש קורא את הקובץ הזה לפני שהוא שופט, כדי
לא לחזור על פידבק שכבר טופל.

## סבב 1 — 2026-07-18 · צילומים: `docs/screenshots/review-r1/`

**VERDICT:** ההירו ברמת Arc/Linear; החולשה הקטלנית — רגע ההמרה מת: כרטיס
המחיר, כרטיסי הפורמטים וכותרת ה־FAQ נעלמים בצילום הדסקטופ המלא (Reveal
תקוע ב־opacity:0).

| # | ממצא | סטטוס |
|---|---|---|
| 1 | Reveal לא fail-visible — תוכן קריטי נעלם | ✅ יושם: מוסתר רק אחרי שה־observer מוכח חי; entries מטופלים במקבץ; no-JS/anchor-jump בטוחים. שורש נוסף שהתגלה: scroll-behavior:smooth הפיל את סקריפט הצילום — תוקן גם הוא |
| 2 | סקשן הייצוא — עמודה קורסת לריק | ✅ נסגר: תסמין של ממצא 1; אומת בצילום ממוקד שהעמודות מיושרות |
| 3 | כפילות CTA מעל הקפל — כפתור הדר צועק | ✅ יושם: outline שקט בהיר מעל ההירו, מילוי מלא אחרי גלילה |
| 4 | פרטיות — חצי במה ריקה | ✅ יושם: פתק ממורכז, padding מקוצץ |
| 5 | מקצב אנכי מדולל פרטיות↔FAQ | ✅ יושם: padding הודק ב־~30% |
| 6 | זוהר כחול נלחם בצילום המוצר הסגול | ✅ יושם: זוהר ניטרלי #FCFCFA + saturate(0.92) |
| M1 | כפתור הדר מובייל בלתי נראה מעל ההירו | ✅ יושם: מוסתר במצב-עליון, מופיע אחרי גלילה (הבדיקה עודכנה בהתאם) |
| M2 | שורת אמון מובייל קטנה וצפופה | ✅ יושם: 13px + text-balance |
| P1 | צילומים מ־dev (עיגול N) | ✅ יושם: צילומים מ־production build על :3101 |
| P2 | gap בין CTA ללינק המשני | ✅ יושם: sm:gap-7 |
| P3 | FAQ סגור שטוח מדי | ✅ יושם: border paper-deep + צל דק |
| P4 | קונוס מנורה חלש במחיר | ✅ יושם: 13%→17%, טווח 76% |
| P5 | פס מרקר חיוור בסיכום המבני | ✅ יושם: bg-marker/25→/45 |

**אימות:** lint ✓ typecheck ✓ build ✓ Playwright 64 passed / 6 skipped ✓
**קומיט:** `5bc1b1d`

## סבב 2 — 2026-07-19 · צילומים: `docs/screenshots/review-r2/`

**VERDICT:** קפיצת מדרגה — כל ששת תיקוני סבב 1 אומתו בצילומים; הדף קוהרנטי
מקצה לקצה. נותרו 4 ממצאים בלבד; "הדף קרוב למיצוי במדיום הסטילס".

| # | ממצא | סטטוס |
|---|---|---|
| 1 | קונוס המנורה בבמת המחיר יושם-אך-לא-נראה | ✅ יושם מחדש: אלומה חמה `#E5B94B` — משולש clip-path עם blur(40px) + זוהר מקור, rim-light זהב על קצה הכרטיס העליון, צל עיגון שחור שנושם בקאונטר-פאזה עם paper-float (`shadow-breathe`); אומת ויזואלית שהקונוס קורא |
| 2 | כתם זוהר שיורי מאחורי כפתור ההדר במצב-עליון | ✅ פוענח: הכתם הוא ראש המנורה בסצנת הווידאו עצמה, לא ארטיפקט DOM. הפתרון — זכוכית כהה עדינה (`rgb(20 20 23/30%)` + blur 3px) לכפתור, שמפרידה אותו נקי מהבוקה; הוחלף גם החוק הישן `.at-top .header-cta` הכחלחל |
| 3 | יישור כותרות לא עקבי (פרטיות ממורכזת מול שכנות start) | ✅ יושם (Sonnet): כותרת הפרטיות חזרה ל־start, הכרטיס נשאר ממורכז; מרכוז שמור לבמות הלילה בלבד |
| 4 | ProductDemo — חור אנכי בטור הכרטיסים | ✅ יושם (Sonnet): `items-center` → `lg:items-start` |
| P1 | ₪ נדבק לספרות ונקרא כ־ש׳ | ✅ יושם: hair space ‏U+200A |
| P2 | קיצוץ ~15% padding אנכי בבמת המחיר ב־1440 | ✅ יושם: `lg:py-24`→`lg:py-20`, ‏`mt-12`→`mt-10` |
| P3 | סבב אימות חי למצבי תנועה/hover | ✅ יושם: `scripts/live-motion-check.mjs` — ‏8 בדיקות (וידאו הירו, זכוכית/מילוי הדר, float+breathe, שכבת קונוס, hover-straighten, FAQ, ‏reduced-motion) — כולן PASS |
| M-האמת | לאמת שכפתור "התקנה" מופיע במובייל אחרי גלילה | ✅ מכוסה בבדיקת Playwright ייעודית מסבב 1 |

**ניהול מודלים (לפי `agent-model-policy.md`):** אורקסטרטור Fable — טריאז',
במת האור, ההדר; Sonnet — ממצאים 3–4 התחומים; Haiku — שרשרת האימות המלאה.

## סבב 3 — 2026-07-19 · צילומים: `docs/screenshots/review-r3/` · מבקר: Opus

**VERIFY:** כל ארבעת תיקוני סבב 2 אומתו כ"נחתו" — קונוס המנורה קורא נקי
(rim-light + צל עיגון), כפתור ההדר מופרד מהמנורה בזכוכית כהה, כותרת
הפרטיות ב־start, ProductDemo מיושר-למעלה.

**VERDICT: הדף התכנס.** "מול הרף של Linear/Vercel/Arc — הדף עומד בו…
מה שנשאר הוא ליטוש, לא ליקוי." מובייל 390px: "מוצק לכל אורכו", אפס
שבירות.

| # | ממצא | סטטוס |
|---|---|---|
| 1 | ProductDemo — חלל לילה ~250px מתחת לכרטיס 3 בדסקטופ (החור עבר מלמעלה למטה) | ✅ יושם: `lg:self-stretch` על הטור + `lg:h-full lg:justify-between` על הרשימה — הכרטיסים מתפזרים לגובה הפאנל; אומת ויזואלית |

**אימות סופי:** lint ✓ typecheck ✓ build ✓ Playwright 64 passed / 6 skipped ✓

**הלולאה נעצרה בהתכנסות** לפי סעיף 7 של חוזה הלולאה
(`agent-model-policy.md`): המבקר הכריז שאין ממצאים מעל רמת ליטוש, והממצא
היחיד שנותר טופל ואומת.

## הפוגה — שינוי מבנה מהותי (agent אחר, 2026-07-19)

לפני סבב 4 הדף עבר שינוי מבנה עצמאי (לא חלק מלולאת הביקורת): צומצם מכל
הסקשנים הקודמים ל־4 מערכות מכריעות בלבד — הירו, ProductDemo (איחד את
"איך זה עובד" + "ההבדל המבני"), Platforms (איחד ייצוא+פלטפורמות), Pricing
(איחד מחיר+FAQ+פרטיות+CTA סופי). ‏76 בדיקות Playwright עברו (כולל בדיקת
רגרסיה חדשה שמגבילה את הדף ל־4 סקשנים). לא קובע — worktree מקומי.

בעקבות זה: כמה טיפולי עומק/תלת-ממד שנבנו בסבבים 1–3 נעלמו (הטיה
תלת-ממדית על פאנל התוסף ב-ProductDemo, ריחוף כרטיס המחיר). שוחזרו
ידנית + נוספו טיפולי עומק חדשים: ערימת "דפים" מאחורי כרטיס הסיכום
ב-ProductDemo, פאנל "לפני" שקוע (inset shadow), הגבהת כרטיסי Platforms
+ תג "B" מרכזי כדיסקית מוגבהת. אומת: lint/typecheck/build/Playwright
76 passed 0 failed (13 כשלים ראשוניים התבררו כשרת dev תקוע — לא קוד).

## סבב 4 — 2026-07-19 · צילומים: `docs/screenshots/review-r4/` · מבקר: Opus

**VERDICT:** הרסטרקצ'ר "מצליח יותר משנכשל" — הסיפור שלם, שום טענה יתומה.
לא התכנס: 2 ממצאי דרג-בינוני.

| # | ממצא | סטטוס |
|---|---|---|
| 1 | סגירת המעגל הקולנועית נעלמה — הדף נעצר שטוח על Pricing→Footer | ✅ יושם: הוחזר `.finale-backdrop` (CSS שנמחק ברסטרקצ'ר, נכס `finale-paper.webp` קיים) + רצועת סגירה קומפקטית *בתוך* `<section id="pricing">` עצמו (לא סקשן חמישי — שומר על בדיקת הרגרסיה ל-4 סקשנים). תקציב גובה נבדק מראש: נותרו רק 315px/480px מרווח לפני חריגה מהתקציב — הרצועה תוכננה בהתאם (הוסר padding-bottom כפול מהסקשן עצמו) |
| 2a | פאנל התוסף ב-ProductDemo קורא שטוח לגמרי בסטילס | ✅ יושם: זווית ההטיה הוגברה מ-9deg ל-17deg (rotateY) — עכשיו קוראת בבירור גם במנוחה, לא רק ב-hover |
| 2b | היפוך מטאפורה: כרטיס "תוצרי הסיכום" (הפתרון) כהה, כרטיס "מקורות" (הכאוס) בהיר — הפוך מהחוק וגם לא עקבי מול ProductDemo | ✅ יושם: הוחלף הטיפול הוויזואלי בין שני הכרטיסים (לא הסדר/המיקום) — תוצרים=נייר חם בהיר, מקורות=כרטיס קריר כהה, עכשיו עקבי עם "נייר חם=הפתרון" בכל הדף |

תופעת לוואי שטופלה: הוספת CTA רביעי (הרצועה) שברה בדיקה שאסרה במפורש `toHaveCount(3)` על ה-CTAs המדודים — הבדיקה עודכנה ל-4 (כולל `final-bookend`), לא עוקפה.

**אימות:** lint ✓ typecheck ✓ build ✓ Playwright 76 passed / 6 skipped ✓
(כולל בדיקת 4-הסקשנים ותקציב אורך הדף — שתיהן עדיין עוברות)

## סבב 5 — 2026-07-19 · צילומים: `docs/screenshots/review-r5/` · מבקר: Opus

**VERIFY:** שלושת תיקוני סבב 4 — כולם "נחתו נקי". רצועת הסיום "לא רק נחתה
אלא החזירה את הרגש הקולנועי שהרסטרקצ'ר שיטח". 17deg "דרמטי אך קריא לגמרי,
לא אגרסיבי מדי". היפוך ה-light/dark ב-Platforms "התיקון הכי משמעותי
מהשלושה... נצמד לחוק נייר-חם=פתרון בכל הדף".

**VERDICT: הדף התכנס.** "מול הרף של Linear/Vercel/Arc — הדף עומד בו...
אפילו סקשן Platforms הבהיר קורא כמעבר מכוון ולא כנפילה."

| # | ממצא | סטטוס |
|---|---|---|
| 1 | תא ריק יתום בגריד "מקורות הרצאה" — Teams לבד בעמודה עם חלל ריק לצידו | ✅ יושם: גם Teams וגם שורת ה-catch-all הפכו ל-col-span-2 (היו רק ה-catch-all) — 6 פריטים ב-2 עמודות + 2 שורות מלאות, אפס תאים יתומים; אומת ויזואלית דסקטופ+מובייל |

**אימות סופי:** lint ✓ typecheck ✓ build ✓ Playwright 76 passed / 6 skipped ✓

**הלולאה נעצרה בהתכנסות.** המבקר לא המציא ממצאים למילוי מכסה — "אין דבר
מעל רמת ליטוש" — והליטוש היחיד טופל ואומת מיד.

## הפוגה 2 — שינויים ידניים אחרי הביקורת (2026-07-19)

לא חלק מלולאת הביקורת: המחיר שונה ל-₪23.90 (מ-9.90), פונט המחיר עבר
מ-Frank Ruhl Libre ל-Assistant (רך יותר), תג "7 ימי התנסות" הוגדל
והודגש (רקע מרקר חזק יותר), טקסט ההירו הוזז לצד שמאל (לחלל הריק מעל
הנייר). **משוב המשתמשת:** "עדיין הדף נחיתה נראה עמוס ומלא".

**ממצא כמותי:** יחס גובה הדף עלה ל-**4.42/4.5** (היה 4.15 בסבב 4) —
כמעט אפס רזרבה לפני חריגה מהתקציב. הסקשן הצפוף ביותר ויזואלית: "המוצר
בפעולה" (ProductDemo) — כותרת + פאנל צילום + 3 כרטיסי שלבים + פאנל
לפני/אחרי, ארבע יחידות תוכן נפרדות בפער מצומצם.

## סבב 6 — 2026-07-19 · צילומים: `docs/screenshots/review-r6/` · מבקר: Opus

**VERDICT:** התלונה מוצדקת — אך לא בגלל אורך הדף (קצר), אלא צפיפות
מרוכזת כמעט כולה בסקשן אחד. שני שורשים: (1) ProductDemo נשא ארבע יחידות
תוכן כבדות (כותרת+פאנל צילום+3 כרטיסי-שלב+פאנל לפני/אחרי) בפערים
כמעט-אחידים — אין "פעימת נשימה" בין קבוצות; (2) שני מלבנים בהירים
(הצילום + נייר הסיכום) התחרו על אותה במה כהה = העין מקפצת ביניהם.

| # | ממצא | סטטוס |
|---|---|---|
| 1 | פאנל לפני/אחרי הוא שחזור רביעי של אותו רעיון (הירו מראה אותו, הכותרת אומרת אותו, הצילום מדגים אותו) | ✅ יושם: **נמחק לגמרי** מ-`ProductDemo.tsx` (לא כווץ — נמחק). שחרר ~260px בדסקטופ, יחס הגובה ירד מ-4.42 ל-**4.13**/4.5 (למרות תוספת ריווח!). מוקד-אור יחיד נשאר (הצילום). `ProductDemo.module.css` (ייעודי לפאנל שנמחק) נמחק כליל |
| 2 | eyebrow כפול על כרטיסי-השלב ("מתקינים/צופים/מקבלים" חוזר על הכותרת מתחתיו) | ✅ יושם: הוסר; המספור בעיגול כבר נותן את הרצף |
| 3 | כל כרטיס-שלב עטוף border+bg+shadow — "מסגרת בתוך מסגרת" מול הבמה הכהה | ✅ יושם: הוסרו הקופסאות; הפרדה בקו-שיער דק (`border-b`) בין הפריטים בלבד, כמו רשימת-פיצ'רים של Linear |
| 4 | מקצב ריווח אחיד (כותרת→תוכן, בין-טורים, בין-כרטיסים כולם באותו סדר גודל) | ✅ יושם: העודף שהשחרור פינה הושקע כריווח נדיב סביב מוקד-האור היחיד (`gap-8`→`gap-14/20`, `mt-9`→`mt-12`) — יותר נושם וגם נמוך יותר בו-זמנית |

מובייל: אושר שהצפיפות שם חמורה עוד יותר (הבלוק הכי כבד בדסקטופ הופך
לבלוק הכי *גבוה* במובייל) — אותם 4 תיקונים פותרים גם אותו, ללא תיקון
נפרד. יחס מובייל ירד ל-**5.52**/6.5.

**מה לא לגעת (אושר רגוע):** ההירו, ההטיה 17deg, רצועת הסיום, קונוס
האור+ריחוף כרטיס המחיר ב-Pricing.

**אימות:** typecheck ✓ lint ✓ build ✓ Playwright 76 passed / 6 skipped ✓

## הפוגה 3 — ניקוי מלל (לא חלק מלולאת הביקורת, 2026-07-19/20)

משוב נוסף מהמשתמשת אחרי סבב 6: "עדיין הדף נחיתה נראה עמוס ומלא" → יושם
(ראו למטה) → "עדיין הדף נחיתה מאוד עמוס בטקסט" (הדגשה מפורשת על טקסט,
לא רק על מבנה/עומק).

**סבב ניקוי 1:** נמחקה כותרת ה-h1 המקורית ("ההרצאה מתקדמת. החומר כבר
מתארגן.") — תיאור אווירה בלי מידע. המשפט האינפורמטיבי שהיה subheadline
קודם ל-h1 עצמו (שומר h1 יחיד בעמוד). נמחקה שורת body מיותרת ב-ProductDemo
(חזרה על הכותרת+3 השלבים). כותרת ה-FAQ "שלוש תשובות. וזה כל מה שצריך
לדעת." פושטה ל"שאלות נפוצות". בדיקה עודכנה בהתאם.

**סבב ניקוי 2 (בעקבות "עדיין... בטקסט"):** ה-h1 עצמו קוצר עוד —
"B Notes מתמלל את ההרצאה בזמן אמת והופך אותה בסיום לסיכום ברור, מחולק
לנושאים ומוכן ללמידה." (3 שורות, 3 תיאורים כמעט-נרדפים לסיכום) →
"B Notes מתמלל את ההרצאה בזמן אמת ומסכם אותה לפי נושאים." (שורה-שתיים,
שתי עובדות בלבד). שלוש שורות ה-body מתחת לכרטיסי השלב ב-ProductDemo
נמחקו לגמרי — הכותרות עצמן ("מתקינים את B Notes"/"צופים בהרצאה
כרגיל"/"מקבלים סיכום מסודר") כבר קוראות כרצף שלם בלי המשפט המסביר
מתחתן. שתי בדיקות עודכנו לנוסח החדש.

`content.ts`'s `howItWorks.steps[].body` ו-`productDemo.title/highlights`
נשארו קיימים אך לא-בשימוש — עדיין מוזנים על ידי `HowItWorks.tsx`/
`CoreValue.tsx`/`FinalCta.tsx`, קומפוננטות יתומות מלפני הרסטרקצ'ר
שלא מיובאות ל-`page.tsx`. לא נגעתי בהן — מחוץ לתחום הניקוי הזה.

**אימות סופי:** typecheck ✓ lint ✓ build ✓ Playwright 76/76 ✓ (כשל
זמני אחד ב"no uncaught errors" התברר כflake של עומס-מקבילי — עבר נקי
בבידוד, לא רגרסיה). יחס גובה הדף: **4.08**/4.5.

## הפוגה 4 — הסרת ה-CTA מההירו (2026-07-19/20)

בקשה מפורשת: להסיר כפתור "התקינו את B Notes ב-Chrome" + המיקרו-קופי
+ הלינק המשני "איך זה עובד ↓" — כל שלושתם, מההירו בלבד (אושר: לא מקום
אחר בדף).

**תלות קריטית שנחשפה:** ל-Header.tsx הייתה לוגיקה קיימת שהסתירה את
כפתור ה-CTA שלו במובייל *כל עוד* המשתמש בראש הדף (`atTop && max-sm:hidden`),
בהנחה מפורשת ("hero's own [CTA] is enough") שכפתור ההירו כבר שם. הסרת
כפתור ההירו בלי לתקן את זה הייתה משאירה משתמשות מובייל בלי שום CTA
נראה מעל לקיפול עד גלילה. תוקן: הוסר ה-`max-sm:hidden` — כפתור ההדר
גלוי בכל הרוחבים, בכל מצב גלילה, מהרגע הראשון.

יתר: `Button`/`cta`/`links` הפכו ל-imports לא-בשימוש ב-Hero.tsx והוסרו;
`cta.secondary` הפך ללא-בשימוש בכל הקוד והוסר מ-`content.ts`.

**בדיקות עודכנו (לא נעקפו):** "hero CTA above the fold" מוזג ל-"header
CTA above the fold" (מכסה גם target/rel/מיקום-קיפול שלא נבדקו קודם על
ההדר); ספירת ה-CTAs הממודדים ירדה מ-4 ל-3 (`header`/`pricing`/
`final-bookend`, בלי `hero`); שתי בדיקות אינטראקציה (focus/click) הוסבו
למקד את כפתור ההדר דרך סלקטור יציב (`header a.header-cta`) במקום טקסט
תלוי-רוחב.

**אימות:** typecheck ✓ lint ✓ build ✓ Playwright 74 passed / 6 skipped ✓
(ירידה מ-76 ל-74 = מיזוג בדיקה אחת, לא כיסוי שאבד). יחס גובה: דסקטופ
4.08 (ללא שינוי), **מובייל ירד מ-5.52 ל-5.07**.
